SELECT 
    FOOD_PRODUCT.PRODUCT_ID AS PRODUCT_ID,
    FOOD_PRODUCT.PRODUCT_NAME AS PRODUCT_NAME,
    (SUM(FOOD_ORDER.AMOUNT) * FOOD_PRODUCT.PRICE) AS TOTAL_SALES
FROM FOOD_PRODUCT 
    INNER JOIN FOOD_ORDER ON FOOD_PRODUCT.PRODUCT_ID = FOOD_ORDER.PRODUCT_ID
WHERE 
    FOOD_ORDER.PRODUCE_DATE BETWEEN '2022-05-01' AND '2022-05-31'
GROUP BY FOOD_PRODUCT.PRODUCT_ID
ORDER BY TOTAL_SALES DESC, FOOD_PRODUCT.PRODUCT_ID;